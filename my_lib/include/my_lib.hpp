#pragma once

/**
 * @brief Used to display versions and
 * a simple Hello World to the standard
 * output
 */
void print_hello_world();

/**
 * @brief Make the factorial of the number parameter
 * @param numer The parameter where the factorial comes from
 * @return std::uint32_t being the factorial of the number
 */
std::uint32_t factorial(std::uint32_t numer);
